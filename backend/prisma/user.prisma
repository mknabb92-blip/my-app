model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  password  String
  position  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @default(now())
}
